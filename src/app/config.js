/**
 * @file config.js
 * @description Global application configuration.
 */
const config = {
    defaultView: 'dashboard',
    appTitle: 'C.U.T.E. Fitness Hub',
    defaultLanguage: 'en',
    supportedLanguages: ['en', 'nl']
};

export default config;